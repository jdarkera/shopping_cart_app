export default function Cart(props){

    return(
        <div>
            {props.prod.map(prop => {
                return (
                    <div>
                        
                    </div>
                ); 
            }) 

            }
        </div>
    );
}